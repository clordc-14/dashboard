# 国药西南新药引进网开发设计文档

## 1. 项目概述

国药西南新药引进网用于展示新药引进、经营与调研相关的报表看板。系统包含两类受控内容：

- **看板数据源**：Excel 文件中的新闻、品种、销售、调研等结构化数据，经解析后用于生成首页、表格页和分析页。
- **AI 分析资料**：PDF、Word、PPT 文件，作为后续向人工智能助手提问时可选择的参考资料。

所有上传文件均存储为私有文件。系统只展示文件元数据和由 Excel 解析得到的结构化页面数据，**不提供任何角色对原始上传文件的下载能力**。

## 2. 建设目标

1. 支持在资料库中保存 Excel、PDF、Word、PPT 文件及其版本。
2. 以 Excel 作为看板的数据源：解析后生成新闻、表格、指标和分析页面所需的结构化数据。
3. 以 PDF、Word、PPT 作为未来人工智能助手的受控分析资料，不直接作为看板数据源。
4. admin 和 editor 可以使用资料库，viewer 只能访问报表看板。
5. 所有已登录角色均可导出当前报表页面为 PDF；该功能导出的是页面内容，不是上传的原始文件。
6. 保持上传文件私有、可审计、可版本化，并支持归档和管理员永久删除。

## 3. 角色与访问权限

### 3.1 角色定义

| 功能 | admin | editor | viewer |
|---|---:|---:|---:|
| 浏览报表看板、表格详情和分析页 | 是 | 是 | 是 |
| 导出当前页面为 PDF | 是 | 是 | 是 |
| 在导航中看到并进入资料库 | 是 | 是 | 否 |
| 读取资料库的文件夹和文件元数据 | 是 | 是 | 否 |
| 新建、重命名文件夹 | 是 | 是 | 否 |
| 上传 Excel/PDF/Word/PPT、上传新版本 | 是 | 是 | 否 |
| 解析并发布 Excel 看板数据 | 是 | 否 | 否 |
| 归档文件、删除文件夹、永久删除归档文件 | 是 | 否 | 否 |
| 下载任意原始上传文件 | 否 | 否 | 否 |

本表保持既有 admin/editor 文件管理边界：editor 可维护文件夹和上传版本，但不具备归档、永久删除或看板发布权限；admin 具备完整管理权限。

### 3.2 viewer 访问边界

viewer 只能使用报表看板、表格详情、分析页和页面 PDF 导出功能。

- 页面导航不得显示“资料库”入口。
- viewer 直接请求资料库页面时，必须被路由守卫拒绝（403）或安全地跳转回看板；不得渲染文件夹、文件名、版本或其他资料库元数据。
- viewer 请求 `/api/folders*`、`/api/files*` 及后续资料解析接口时必须返回 403。
- 仅隐藏按钮不是授权措施；服务端必须再次校验当前用户角色。

## 4. 文件类型、用途与生命周期

### 4.1 支持的文件类型

| 类别 | 扩展名 | 主要用途 | 是否用于看板解析 |
|---|---|---|---:|
| Excel | `.xlsx`、`.xls` | 看板数据源、新闻/表格/指标/分析数据 | 是 |
| PDF | `.pdf` | 后续人工智能助手的参考资料 | 否 |
| Word | `.docx` | 后续人工智能助手的参考资料 | 否 |
| PPT | `.pptx` | 后续人工智能助手的参考资料 | 否 |

第一阶段不接收旧式 `.doc`、`.ppt`；如业务需要支持，应单独增加格式转换与安全验证方案。单文件大小默认限制为 20 MB，实际限制以服务端配置为准。

### 4.2 文件状态

1. **已上传**：文件及版本以私有对象保存，资料库显示名称、类型、大小、版本和更新时间。
2. **已归档**：仅 admin 可查看；保留私有对象和版本，不在常规文件列表中显示。
3. **已永久删除**：仅 admin 可对归档文件执行；删除全部私有对象、文件记录和版本记录，保留审计日志。

无论处于何种状态，均不提供下载原文件的按钮、接口、公开 URL 或预签名 URL。

## 5. 页面与交互设计

### 5.1 报表看板

所有已登录用户均可访问首页、表格详情和分析页。页面展示已发布 Excel 数据生成的内容，包括：

- 本周重点信息、上周上市创新药回顾等新闻板块；
- 品种、引进协助、销售和调研等表格板块；
- 基于结构化数据生成的指标、趋势和分析视图；
- “导出页面 PDF”按钮。

PDF 导出仅包含当前渲染的看板、筛选结果和表格内容；不得嵌入、附带或链接任何原始上传文件。

### 5.2 资料库

资料库只向 admin 和 editor 展示。文件必须归属某个文件夹，根目录只用于组织文件夹，不提供直接上传入口。

资料库应展示：

- 文件夹层级与面包屑；
- 文件名称、类型、大小、当前版本、状态和更新时间；
- admin/editor 可执行的既有文件夹与上传操作；
- admin 专属的归档列表、归档和永久删除操作。

资料库不展示原始文件正文、R2 对象键、公开地址或下载按钮。

### 5.3 Excel 数据源的解析与发布

上传 Excel 到资料库后，文件只处于“已上传”状态，不应自动覆盖全站看板。推荐流程如下：

1. admin 在资料库选择一个 Excel 文件的当前版本。
2. 系统在服务端权限校验后读取该私有对象并解析工作簿。
3. 按配置匹配 sheet、标题行和业务板块，清洗并规范化数据。
4. 系统返回解析摘要、识别到的板块数量、缺失字段和错误信息，供 admin 核对。
5. admin 明确执行“发布到看板”后，系统将该次解析结果作为当前已发布数据版本。
6. 首页、表格页和分析页统一读取已发布的结构化数据，而非直接读取原始 Excel。

发布操作属于既有看板数据维护权限，仅 admin 可执行。editor 可以上传新的 Excel 或版本，供 admin 审核并发布；viewer 只能查看已发布的页面结果。

### 5.4 PDF、Word、PPT 与人工智能助手

PDF、Word、PPT 上传后先作为私有资料保存，不自动解析进看板，也不自动发送给外部 AI 服务。

后续人工智能助手功能应支持由有资料库权限的用户选择可访问的资料作为提问上下文。服务端在每次提问时必须：

1. 校验用户仍具备资料库访问权限；
2. 读取私有对象并进行对应格式的文本提取；
3. 按最小必要范围向模型提供提取后的内容或检索片段；
4. 记录资料引用与提问审计，但不把原始文件 URL、R2 键或全文下载链接返回浏览器；
5. 对已归档或已删除文件执行相同的访问限制。

viewer 不得选择、检索或向人工智能助手提交资料库文件。

## 6. Excel 数据解析设计

### 6.1 解析规则

解析器应复用配置化的板块映射，而不是将 sheet 名和列名写死在页面逻辑中。每个板块至少包含：

```javascript
{
  key: "weeklyFocus",
  title: "本周重点信息",
  matchTitle: "本周重点信息",
  type: "news"
}
```

解析时应：

1. 识别有效工作表、标题区域和表头；
2. 跳过空白行、说明行，处理合并单元格；
3. 统一日期、金额、状态等字段格式；
4. 校验新闻链接仅为合法的 `http` 或 `https` 地址；
5. 输出新闻板块、表格板块、指标与分析页所需的结构化结果；
6. 在模板不匹配、表头缺失或数据异常时提供可定位的错误摘要。

### 6.2 数据版本

系统应区分“文件版本”“解析版本”和“已发布版本”：

- 文件版本用于保留原始私有 Excel 的历史；
- 解析版本用于记录某个文件版本的解析结果与校验状态；
- 已发布版本是当前所有人看到的看板数据来源。

新上传或新解析的数据不得影响正在使用的已发布看板，直到 admin 确认发布。后续可增加将看板回退到历史解析版本的能力。

## 7. 技术架构

### 7.1 前端

- HTML、CSS、JavaScript 与 Vite：页面、交互和构建；
- SheetJS `xlsx`：Excel 工作簿解析逻辑；
- 页面 PDF 导出：基于已渲染页面内容生成，不读取原始附件；
- 前端只渲染服务端授权返回的数据，所有动态文本均需转义。

### 7.2 Cloudflare 服务端

- Cloudflare Pages + Functions：提供认证、RBAC、资料库、解析和看板数据接口；
- Cloudflare Access：生产与预览环境的身份认证；
- D1：用户、文件元数据、文件版本、解析版本、已发布数据指针和审计日志；
- R2：私有保存上传文件及历史版本；
- 后续 AI 服务：仅通过受控服务端接口处理已授权资料，浏览器不得直接访问对象存储。

## 8. 接口与数据边界

### 8.1 资料库接口

资料库相关接口必须要求 admin 或 editor 身份。包括文件夹查询、文件列表、文件上传、文件版本和后续 AI 资料选择接口。

- 写操作必须校验同源请求与正确的 Content-Type；
- 所有数据库查询使用参数绑定；
- 响应只返回必要的文件元数据，不返回 `r2_key`、对象 URL、下载 URL、访问令牌或原始文件字节；
- viewer 对资料库接口一律收到 403；
- admin 对归档文件的访问和永久删除必须记录审计日志。

### 8.2 看板数据接口

看板和分析接口向所有已登录角色开放，但只能读取已发布的结构化数据。接口不得根据前端参数直接读取任意 R2 文件。

## 9. 安全与隐私要求

1. R2 桶必须为私有桶，不绑定公开访问域名。
2. 不实现 `/download` 类接口，不生成公开 URL 或预签名下载 URL。
3. 页面、API 响应、错误信息与审计展示中不得泄露本地路径、R2 对象键、访问令牌或原始附件地址。
4. 表格详情、指标、图表与 PDF 导出均基于结构化看板数据，不直接打开或下载 Excel 原件。
5. 文件名、文件夹名、姓名、邮箱和 API 文本插入页面前必须转义。
6. 文件大小、扩展名和 MIME 类型必须在服务端校验；必要时增加恶意文件扫描与内容识别。
7. 归档不是删除；永久删除只能针对归档文件执行，并同时清理其所有私有版本对象与 D1 元数据，保留审计事件。
8. AI 提问必须遵循最小权限和最小数据范围原则；未经授权的用户与模型调用不得获得资料库原始内容。

## 10. 核心模块

| 模块 | 职责 |
|---|---|
| `excelParser` | 读取 Excel、提取工作表和单元格数据、输出中间工作簿结构。 |
| `sectionMatcher` | 按配置匹配新闻和表格板块。 |
| `normalizer` | 清洗空行、合并单元格、日期、字段名和链接。 |
| 看板数据服务 | 保存解析版本、控制发布状态，并为所有看板页面提供已发布结构化数据。 |
| 资料库服务 | 管理私有文件、版本、归档、永久删除与审计。 |
| AI 资料服务（后续） | 在权限校验后提取 PDF/Word/PPT 文本并为提问提供受控上下文。 |
| 页面 PDF 导出模块 | 导出当前看板页面，不处理或附带原始上传附件。 |

## 11. 验收标准

1. admin、editor、viewer 均可浏览看板并导出当前页面 PDF。
2. viewer 看不到资料库入口；直接访问资料库页面或资料库 API 时不能取得任何资料库内容。
3. admin 和 editor 可在文件夹内上传 `.xlsx`、`.xls`、`.pdf`、`.docx`、`.pptx` 文件；根目录不能直接上传。
4. admin/editor 的既有资料库管理差异保持不变：editor 无归档、永久删除和发布权限。
5. 系统不对任何角色显示原始文件下载按钮、原始文件链接、R2 地址或预签名 URL。
6. Excel 可以产生可校验的解析结果；只有 admin 明确发布后才更新全站看板数据。
7. PDF、Word、PPT 不会自动改变看板；后续 AI 分析使用时必须经过资料权限校验。
8. admin 归档和永久删除文件后，文件状态、私有对象、版本元数据和审计记录符合生命周期要求。
9. 所有资料库写操作、解析发布操作和 AI 资料引用均有可追溯审计。
10. PDF 导出只包含页面渲染结果，不包含任何原始上传文件。

## 12. 后续实施优先级

1. 完成 viewer 的资料库页面与 API 路由隔离。
2. 扩展资料库文件类型校验，加入 PDF、DOCX、PPTX。
3. 建立 Excel 私有文件的“解析、校验、发布”流程与结构化数据版本。
4. 让首页、表格页与分析页改为读取已发布的服务端数据，而不是浏览器本地缓存。
5. 为 PDF、Word、PPT 实现受控文本提取、检索和人工智能助手资料选择功能。

